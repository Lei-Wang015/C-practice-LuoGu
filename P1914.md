```C
#include <stdio.h>
#include <string.h>

int main ()
{
	int n = 0;
	scanf("%d", &n);
	char str[51];
	scanf("%s", str);
	for(int i = 0; str[i] != '\0';i++){
		str[i] = (str[i] - 'a' + n) % 26 + 'a';
	}
	printf("%s\n", str);
	return 0;
}
```
- 计算偏移量别忘了求余

```C
#include <stdio.h>
#include <string.h>

int get_value(char *choice);

int main() {
	int num = 0;
	scanf("%d", &num);
	for(int i = 0; i < num; i++){
		char p1[20], p2[20];
		scanf("%s %s", p1, p2);
		int val1 = get_value(p1);
		int val2 = get_value(p2);
		int diff = val1 - val2;
		if(diff == 1 ||diff == -2){
			printf("Player1\n");
		}
		else if(diff == 0){
			printf("Tie\n");
		}
		else{
			printf("Player2\n");
		}
	}
	return 0;
}

int get_value(char *choice){
	if(strcmp(choice, "Scissors") == 0){
		return 1;
	}
	else if(strcmp(choice, "Paper") == 0){
		return 0;
	}
	else if(strcmp(choice, "Rock") == 0){
		return -1;
	}
	return 0;
}
```

- 不敢相信是我写出来的代码

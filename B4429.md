```C
#include <stdio.h>

int main ()
{
	int n,m,r,c;
	scanf("%d %d %d %d", &n, &m, &r, &c);
	int x1,y1,x2,y2;
	scanf("%d %d %d %d", &x1, &y1, &x2, &y2);
	int result = (x2 / r - x1 / r + 1) * (y2 / c - y1 / c + 1);
	printf("%d\n", result);
	return 0;
}
```

```C
#include <stdio.h>
#include <string.h>

int is_palindrome(char *s, int start, int end);

int main ()
{
	int n = 0;
	scanf("%d", &n);
	for(int i = 0;i < n; i++){
		char str[101];
		scanf("%s", str);
		int len = strlen(str);
		int isFind = 0;
		for(int j = 2;j < len - 1; j++){
			if(is_palindrome(str,0,j - 1) && is_palindrome(str, j, len - 1)){
				isFind = 1;
				break;
			}
		}
		if(1 == isFind){
			printf("Yes\n");
		}
		else{
			printf("No\n");
		}
	}
	return 0;
}


int is_palindrome(char *s, int start, int end){
	if(start > end){
		return 0;
	}
	while(start < end){
		if(s[start] != s[end]){
			return 0;
		}
		start ++;
		end --;
	}
	return 1; 
}
```

- 对哦，只分两段的话可以移动间断点，好聪明

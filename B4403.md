```
#include <stdio.h>
int main ()
{
	int hour = 0, min = 0;
	scanf("%d %d", &hour, &min);
	int mod = min % 5;
	if(mod > 3){
		printf("%d\n", 8 - mod);
	}
	else if(mod <= 3){
		printf("%d\n", 3 - mod);
	}
	return 0;
}
```
